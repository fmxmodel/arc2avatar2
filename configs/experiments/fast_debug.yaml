# Arc2Avatar — Full calibration run
# Reduced from paper defaults (500/2000) for faster turnaround

stage1:
  iterations: 100

stage2:
  iterations: 200

animation:
  expressions: ["neutral"]
  refinement_iterations: 5

manual_approval:
  require_approval_after_finetune: false
  require_approval_after_stage1: false
  require_approval_after_stage2: false
  require_approval_after_refinement: false
